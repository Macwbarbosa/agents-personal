# agents-personal

## Configuracao local

1. Copie `.env.example` para `.env`
2. Preencha os valores reais no `.env`
3. Rode `python3 dashboard/server.py` ou `uv run --with google-api-python-client --with google-auth dashboard/sync.py`
